from setuptools import setup, find_packages

setup(
    name="steps_cell_signaling",
    version="0.1",
    packages=find_packages(),
    # install_requires=[
    #     "numpy",
    #     "pandas",
    #     "mpi4py",
    # ],
) 